# P16 — Online infra (SAM + CI + DNS)

> **Status:** queued. **Depends on:** P14. **Unblocks:** P17–P19.

## Intent

AWS SAM skeleton: private S3 bucket, HTTP API, WebSocket API, Lambdas, IAM,
ACM hooks; GitHub Actions OIDC deploy; Namecheap CNAME checklist for
`api.games.hochgi.com` and `ws.games.hochgi.com`.

## Out of scope

Business handlers (P17–P18), Pages Sign-In UI (P19).
