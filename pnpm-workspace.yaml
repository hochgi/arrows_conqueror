packages:
  - 'packages/*'

# pnpm 11: only these packages may run lifecycle scripts.
onlyBuiltDependencies:
  - esbuild
  - lefthook

# Result of `pnpm approve-builds` for those binaries.
allowBuilds:
  esbuild: true
  lefthook: true
